
int cu_gz_slurp_line(char **tokens, gzFile f, char delim)
{
    int BUFFER_SIZE = 32;


}


char **split(char *line, char delim)


void read_counts(char *path)
{
    char buffer[1000];
    gzFile f = NULL;
    f = gzopen(path, "r");
    if (f!=NULL) {
    
        int read = gzread()
        buffer[read] = '\0';

    }
    gzclose(f);
}

int main(int argc, char **argv)
{



}
